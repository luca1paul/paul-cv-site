[
 {example_app, [
   {log_level, info},
   {region, "staging-eu"},
   {limits, [{max_conn, 5000}, {max_req_per_min, 120000}]},
   {upstreams, ["proxy.alpha:8080", "metrics.local:9090", "audit.local:9200"]}
 ]}
].
%% keyword: proxy.alpha
