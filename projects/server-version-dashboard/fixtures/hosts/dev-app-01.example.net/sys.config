%% Example application config (Erlang-style)
[

  {app_core, [
    {log_level, info},
    {env, "dev"},
    {feature_flags, [safe_mode, audit_log]},
    %% proxy.alpha
    {endpoints, [
      {primary, "https://api.dev.example.net"},
      {secondary, "https://api-backup.dev.example.net"}
    ]},

    %% Port is inside external_access_additional_fields (what the script parses)
    {external_access_additional_fields, [
      {proto, tcp},
      {port, 5060},
      {tls, false}
    ]}
  ]},

  {app_net, [
    {dns_timeout_ms, 1500},
    {connect_timeout_ms, 2000},
    {retry, {linear, 3, 500}}
  ]}
].
