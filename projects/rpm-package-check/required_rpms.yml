# Required RPMs Configuration
# This YAML file lists the required RPM packages for the application.
# Located at: projects/rpm-package-check/required_rpms.yml

---
required_rpms:
  - app-core
  - app-agent
  - app-proxy
